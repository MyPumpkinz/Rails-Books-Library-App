<h2> <%= @book.title %></h2>
<h2> <%= @book.description %></h2>
<h2> <%= @book.author %></h2>

<%= link_to "Back", root_path%>
<%= link_to "Edit", edit_book_path(@book) %>
<%= link_to "Delete", book_path(@book), method: :delete, data: { turbo_confirm: 'Are you sure?' } %>